import { createEdgeApplication } from "./models/EdgeApplication.model"
import { createEdgeFunctions } from "./models/EdgeFunction.model"
import { createToken } from "./models/Tokens.models"

export default {
    createEdgeApplication,
    createEdgeFunctions,
    createToken
}
